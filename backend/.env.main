# ================================ Database ================================

DB_USER="root"
DB_PASSWORD="root"
DB_HOST="localhost"
DB_PORT="5432"
DB_NAME="PersonalKids"

DATABASE_URL="postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# ================================ Prisma ================================

PRISMA_PORT="5555"

# ================================ Backend ================================

BACK_PORT="6011"